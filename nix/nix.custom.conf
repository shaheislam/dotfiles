# Managed by dotfiles — ~/dotfiles/nix/nix.custom.conf
# Symlinked to /etc/nix/nix.custom.conf by scripts/setup.sh
# Included by /etc/nix/nix.conf via: !include nix.custom.conf
trusted-users = root @admin

# Machine-specific overrides (VPN certs, proxy settings, local substituters).
# This file is NOT in dotfiles — edit /etc/nix/nix.machine.conf directly.
!include nix.machine.conf
