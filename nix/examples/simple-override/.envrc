# Simple example: Use global Nix profile with no overrides
# This gives you all the global LSPs

use flake ~/dotfiles/nix/global

# This is useful when you want:
# - All global tools available
# - No specific version overrides
# - Just the baseline environment