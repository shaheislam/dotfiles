# Activate the Node.js project flake
use flake

# Optional: Set Node-specific environment variables
export NODE_ENV=development

# Add node_modules/.bin to PATH for project tools
PATH_add node_modules/.bin

# Optional: Use project's node version if .nvmrc exists
# if [ -f .nvmrc ]; then
#   use node
# fi