# Activate the Python project flake
use flake

# Optional: Set Python-specific environment variables
export PYTHONDONTWRITEBYTECODE=1
export PYTHONUNBUFFERED=1

# Optional: Auto-activate virtual environment if it exists
if [ -d .venv ]; then
  source .venv/bin/activate
fi