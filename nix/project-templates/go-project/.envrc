# Activate the Go project flake
use flake

# Optional: Set Go-specific environment variables
export GO111MODULE=on
export GOPATH="$HOME/go"

# Add project bin to PATH for locally built tools
PATH_add ./bin

# Optional: Set project-specific Go proxy
# export GOPROXY=https://proxy.golang.org,direct