<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Proactive agent monitor: triage all active worktrees every 180s -->
    <!-- Install: launchctl load ~/Library/LaunchAgents/com.dotfiles.agent-monitor.plist -->
    <!-- Remove:  launchctl unload ~/Library/LaunchAgents/com.dotfiles.agent-monitor.plist -->

    <key>Label</key>
    <string>com.dotfiles.agent-monitor</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>-c</string>
        <string>exec $HOME/dotfiles/scripts/agent-triage.sh --all --quiet</string>
    </array>

    <!-- Periodic check every 180 seconds (3 minutes) -->
    <key>StartInterval</key>
    <integer>180</integer>

    <key>StandardOutPath</key>
    <string>/tmp/agent-monitor-stdout.log</string>

    <key>StandardErrorPath</key>
    <string>/tmp/agent-monitor-stderr.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
		<key>HOME</key>
		<string>__HOME__</string>
	</dict>
</dict>
</plist>
