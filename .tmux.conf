# Basic tmux configuration
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*:Tc"
set -ga terminal-overrides ",*:RGB"
set -ga terminal-overrides ",xterm-256color:Ms=\\E]52;c;%p2%s\\7"
set -ga terminal-overrides ",tmux-256color:Ms=\\E]52;c;%p2%s\\7"

# Set prefix to Ctrl-s (more ergonomic than Ctrl-Space)
set -g prefix C-s
unbind C-b
unbind C-a
# Unbind C-z to ensure it's passed through to nested applications (like Neovim terminal)
unbind C-z
# Double tap Ctrl-s to send literal Ctrl-s to applications
bind-key C-s send-prefix

# Enable key repeat
set -g repeat-time 0

# Basic settings
set -g mouse on
set -g escape-time 0
set -g focus-events on
set -g history-limit 1000000
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# Activity monitoring - custom completion detection
# Monitors activity, then checks for completion after silence period
setw -g monitor-activity on
set -g visual-activity off  # Don't show visual alerts
set -g activity-action other  # Only trigger on OTHER windows, not current
# Disable visual styling for windows with activity (no grey background)
set -g window-status-activity-style "fg=#6c7086,bg=#1e1e2e"

# Disable built-in silence monitoring (we use custom detection instead)
setw -g monitor-silence 0
set -g visual-silence off

# Hook to detect activity completion (activity followed by silence)
set-hook -g alert-activity 'run-shell "~/dotfiles/scripts/tmux/tmux-activity-monitor.sh #{session_name} #{window_index}"'

# Hook to clear bell when switching to a window
set-hook -g after-select-window 'run-shell "~/dotfiles/scripts/tmux/tmux-activity-clear.sh #{session_name} #{window_index}"'

# Bell on activity completion - disabled
set -g bell-action none
set -g visual-bell off  # Disable visual bell flash

# Clipboard integration
set -g set-clipboard on
# Allow OSC-52 escape sequences to pass through to the terminal
# Required for Neovim clipboard to work in SSH/container sessions
set -g allow-passthrough on

# OS-specific default shell and clipboard configuration
# macOS: Use Homebrew fish with reattach-to-user-namespace for clipboard access
if-shell "uname | grep -q Darwin" \
  "set -g default-shell /opt/homebrew/bin/fish; \
   set -g default-command 'reattach-to-user-namespace -l fish'" \
  "set -g default-shell /usr/bin/fish"

# Enhanced Copy Mode Configuration
# Use vi-style keys in copy mode
setw -g mode-keys vi

# Easy copy mode entry with prefix
# Use v with prefix (Ctrl-s v) instead of [
bind-key v copy-mode
# Keep the default [ binding as well
bind-key [ copy-mode

# Copy mode key bindings (vi-style)
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
# Stay in copy mode after yanking (use copy-pipe instead of copy-pipe-and-cancel)
# OS-specific clipboard command: pbcopy on macOS, xclip on Linux
if-shell "uname | grep -q Darwin" \
  "bind-key -T copy-mode-vi y send-keys -X copy-pipe 'pbcopy'; \
   bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'pbcopy'; \
   bind-key -T copy-mode-vi Enter send-keys -X copy-pipe 'pbcopy'" \
  "bind-key -T copy-mode-vi y send-keys -X copy-pipe 'xclip -selection clipboard'; \
   bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'xclip -selection clipboard'; \
   bind-key -T copy-mode-vi Enter send-keys -X copy-pipe 'xclip -selection clipboard'"

# Additional convenient bindings for copy mode
bind-key -T copy-mode-vi Space send-keys -X begin-selection
bind-key -T copy-mode-vi Escape send-keys -X cancel
# Double-click to select word, triple-click to select line
bind-key -T copy-mode-vi DoubleClick1Pane select-pane \; send-keys -X select-word
bind-key -T copy-mode-vi TripleClick1Pane select-pane \; send-keys -X select-line

# Search bindings in copy mode
bind-key -T copy-mode-vi / command-prompt -i -p "search down" "send -X search-forward-incremental \"%%%\""
bind-key -T copy-mode-vi ? command-prompt -i -p "search up" "send -X search-backward-incremental \"%%%\""

# Key bindings
# Reload configuration
bind r source-file ~/.tmux.conf \; display-message "Configuration reloaded"

# Window management
bind c new-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind '"' split-window -v -c "#{pane_current_path}"
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Smart adaptive splitting - automatically chooses horizontal or vertical based on pane dimensions
bind -r Space run-shell " \
  if [[ \$(($(tmux display -p '8*#{pane_width}-20*#{pane_height}'))) -lt 0 ]]; then \
    tmux splitw -v -c '#{pane_current_path}' && \
    tmux display '#[fg=#a6e3a1]↔ Split Horizontal#[default]'; \
  else \
    tmux splitw -h -c '#{pane_current_path}' && \
    tmux display '#[fg=#89dceb]↕ Split Vertical#[default]'; \
  fi"

# Respawn pane - restart a dead/frozen process in current pane
bind R respawn-pane -k \; display '#[fg=#a6e3a1] Pane Respawned#[default]'

# Manually clear activity notification for current window (auto-clears on window switch)
bind M run-shell "~/dotfiles/scripts/tmux/tmux-activity-clear.sh #{session_name} #{window_index}" \; \
  display '#[fg=#6c7086] Activity notification cleared#[default]'

# Toggle logging - save pane output to a file (press again to stop)
# Changed from P to L to avoid conflict with floax menu
bind L pipe-pane -o "cat >> ~/dotfiles/tmux-logs/\$(date +%Y%m%d-%H%M)-#W.log" \; \
  display '#[fg=#f9e2af]📝 Logging: ~/dotfiles/tmux-logs/\$(date +%Y%m%d-%H%M)-#W.log#[default]'

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
# set -g @plugin 'alexwforsythe/tmux-which-key'  # Disabled - conflicts with Space key binding
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'tmux-plugins/tmux-open'
# Configure tmux-open
set -g @open-S 'https://www.google.com/search?q='
# Use 'o' to open URLs/files in copy mode
set -g @open 'o'
# Use Ctrl-o to open in editor
set -g @open-editor 'C-o'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-pain-control'
set -g @plugin 'tmux-plugins/tmux-sidebar'
set -g @plugin 'Morantron/tmux-fingers'
# tmux-fingers configuration - easy copy/paste from pane content
set -g @fingers-key F
# OS-specific clipboard command for tmux-fingers
if-shell "uname | grep -q Darwin" \
  "set -g @fingers-main-action 'pbcopy'" \
  "set -g @fingers-main-action 'xclip -selection clipboard'"
set -g @fingers-ctrl-action ':open:'
set -g @fingers-shift-action ':paste:'
set -g @fingers-hint-style 'fg=yellow,bold'
set -g @fingers-highlight-style 'fg=yellow'
set -g @fingers-show-copied-notification 1

# tmux-fzf configuration - unified FZF menu for tmux operations
# Access with: Ctrl-s + f (Find/FZF menu)
TMUX_FZF_LAUNCH_KEY="f"
TMUX_FZF_OPTIONS="-p -w 80% -h 80% -m"
TMUX_FZF_ORDER="session|window|pane|command|keybinding|clipboard"

# tmux-battery removed - was loaded but never displayed (OPTIMIZED)
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'omerxx/tmux-floax'
# Catppuccin Mocha Theme with omerxx-inspired aesthetics

# Session Management - Single unified manager with tmuxinator integration
# Access with: Ctrl-s + S
bind S display-popup -E -h 80% -w 80% "$HOME/dotfiles/scripts/tmux/tmux-session-manager.sh"

# Session Trash Recovery - Browse and restore killed sessions
# Access with: Ctrl-s + T
bind T display-popup -E -h 80% -w 80% "$HOME/dotfiles/scripts/tmux/tmux-session-trash.sh restore"

# Which-key configuration (disabled - conflicts with Space binding)
# set -g @which-key-popup 1

# Floax configuration - floating panes with Catppuccin Mocha colors
set -g @floax-bind 'w'  # Toggle floating pane (using 'w' for window)
set -g @floax-bind-menu 'W'  # Capital W for the menu
set -g @floax-width '80%'
set -g @floax-height '80%'
set -g @floax-border-color '#cba6f7'  # Catppuccin Mocha mauve
set -g @floax-text-color '#cdd6f4'    # Catppuccin Mocha text

# Minimal tmux status bar configuration
# Ultra-clean aesthetic with only window tabs
set -g status-position bottom
set -g status-style "bg=#1e1e2e,fg=#cdd6f4"
set -g status-justify centre

# Status bar left side - completely empty
set -g status-left-length 0
set -g status-left ""

# Status bar right side - CPU and RAM metrics (OPTIMIZED: single location instead of per-window)
# This reduces script calls from 2*N (per window) to just 2 per refresh cycle
set -g status-right-length 50
set -g status-right "#[fg=#45475a]│#[fg=#6c7086] 󰻠 #(~/.tmux/plugins/tmux-cpu/scripts/cpu_percentage.sh) 󰍛 #(~/.tmux/plugins/tmux-cpu/scripts/ram_percentage.sh) "

# Window status - minimal with simple pipe separators (OPTIMIZED: removed shell commands)
# Inactive windows: dark background with gray text
set -g window-status-format "#[fg=#6c7086,bg=#1e1e2e] #I: #W "
# Active window: Rose Pine rose (light pink) background with dark text
set -g window-status-current-format "#[fg=#191724,bg=#ebbcba,bold] #I: #W "
# Separator between windows
set -g window-status-separator "#[fg=#45475a]│"

# Pane borders - subtle with accent on active
set -g pane-border-style "fg=#313244"
set -g pane-active-border-style "fg=#cba6f7"

# Message style
set -g message-style "bg=#313244,fg=#cdd6f4"
set -g message-command-style "bg=#313244,fg=#cdd6f4"

# Copy mode colors
set -g mode-style "bg=#45475a,fg=#cdd6f4"

# Update status bar every 30 seconds for CPU/RAM (OPTIMIZED: reduced from 10s)
# This reduces refresh overhead by 3x while still providing timely updates
set -g status-interval 30

# Auto-install TPM if not present
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# Automatically split new windows and run claude (DISABLED)
# This creates a vertical split with claude in the right pane for every new window
# set-hook -g after-new-window 'split-window -h -p 40 "claude"'

# Manual keybinding to open Claude Code when needed
# Prefix + C opens Claude in a vertical split (40% width on the right)
bind C split-window -h -p 40 "claude"

# Quick launch: Ctrl-s a opens Claude with smart adaptive split
bind a run-shell " \
  if [[ \$(($(tmux display -p '8*#{pane_width}-20*#{pane_height}'))) -lt 0 ]]; then \
    tmux splitw -v -c '#{pane_current_path}' 'claude' && \
    tmux display '#[fg=#a6e3a1]↔ Split Horizontal (Claude)#[default]'; \
  else \
    tmux splitw -h -c '#{pane_current_path}' 'claude' && \
    tmux display '#[fg=#89dceb]↕ Split Vertical (Claude)#[default]'; \
  fi"

# Quick launch: Ctrl-s e opens Neovim in current directory
bind e send-keys "nvim ." C-m

# URL handling - open URLs from tmux pane content
# Prefix + u: open URLs from visible pane content (changed from 'o' to avoid conflict)
# Prefix + U: open URLs from full scrollback history
bind-key u run-shell -b "$HOME/dotfiles/scripts/tmux/tmux-url-handler.sh"
bind-key U run-shell -b "$HOME/dotfiles/scripts/tmux/tmux-url-handler.sh full"

# Custom binding for opening URLs with Ctrl+s, o
# This opens a list of URLs found in the current pane
bind-key o run-shell -b "$HOME/dotfiles/scripts/tmux/tmux-url-handler.sh"

# Keep default tmux bindings for navigation
# Prefix + O: select previous pane
# Prefix + p: previous window (default tmux behavior)

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# Force tmux-open bindings after all plugins load
run-shell '~/.tmux/plugins/tmux-open/open.tmux'
