# Basic tmux configuration
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*:Tc"
set -ga terminal-overrides ",*:RGB"
set -ga terminal-overrides ",xterm-256color:Ms=\\E]52;c;%p2%s\\7"
set -ga terminal-overrides ",tmux-256color:Ms=\\E]52;c;%p2%s\\7"

# Set prefix to Ctrl-Space
set -g prefix C-Space
unbind C-b
unbind C-a
bind-key -n C-Space send-prefix
bind-key C-Space send-prefix

# Enable key repeat
set -g repeat-time 0

# Basic settings
set -g mouse on
set -g escape-time 0
set -g focus-events on
set -g history-limit 1000000
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# Clipboard integration
set -g set-clipboard on

# Use reattach-to-user-namespace for clipboard access
set -g default-shell /opt/homebrew/bin/fish
set -g default-command "reattach-to-user-namespace -l fish"

# Enhanced Copy Mode Configuration
# Use vi-style keys in copy mode
setw -g mode-keys vi

# Copy mode key bindings (vi-style)
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"
bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "pbcopy"

# Key bindings
# Window management
bind c new-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind '"' split-window -v -c "#{pane_current_path}"
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Smart adaptive splitting - automatically chooses horizontal or vertical based on pane dimensions
bind -r Space run-shell " \
  if [[ \$(($(tmux display -p '8*#{pane_width}-20*#{pane_height}'))) -lt 0 ]]; then \
    tmux splitw -v -c '#{pane_current_path}' && \
    tmux display '#[fg=#50fa7b]↔ Split Horizontal#[default]'; \
  else \
    tmux splitw -h -c '#{pane_current_path}' && \
    tmux display '#[fg=#8be9fd]↕ Split Vertical#[default]'; \
  fi"

# Respawn pane - restart a dead/frozen process in current pane
bind R respawn-pane -k \; display '#[fg=#50fa7b]✨ Pane Respawned#[default]'

# Toggle logging - save pane output to a file (press again to stop)
bind P pipe-pane -o "cat >> ~/dotfiles/tmux-logs/\$(date +%Y%m%d-%H%M)-#W.log" \; \
  display '#[fg=#f1fa8c]📝 Logging: ~/dotfiles/tmux-logs/\$(date +%Y%m%d-%H%M)-#W.log#[default]'

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'alexwforsythe/tmux-which-key'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-pain-control'
set -g @plugin 'tmux-plugins/tmux-sidebar'
set -g @plugin 'Morantron/tmux-fingers'
set -g @plugin 'dracula/tmux'

# Session Management - Single unified manager with tmuxinator integration
# Access with: Ctrl-Space + S
bind S display-popup -E -h 80% -w 80% "$HOME/dotfiles/scripts/tmux-session-manager.sh"

# Session Trash Recovery - Browse and restore killed sessions
# Access with: Ctrl-Space + T
bind T display-popup -E -h 80% -w 80% "$HOME/dotfiles/scripts/tmux-session-trash.sh restore"

# Hook to save sessions to trash before they're killed
set-hook -g session-closed 'run-shell "$HOME/dotfiles/scripts/tmux-session-trash.sh save #{session_name}"'

# Which-key configuration
set -g @which-key-popup 1

# Dracula theme configuration
set -g @dracula-show-powerline true
set -g @dracula-plugins "cpu-usage ram-usage battery time"
set -g @dracula-show-weather false
set -g @dracula-show-location false
set -g @dracula-show-fahrenheit false
set -g @dracula-show-flags true
set -g @dracula-show-left-icon session
set -g @dracula-cpu-usage-label " "
set -g @dracula-ram-usage-label " "
set -g @dracula-battery-label " "
set -g @dracula-show-battery-icon false
set -g @dracula-time-format "%l:%M"
set -g @dracula-show-timezone false
set -g @dracula-day-month false
set -g @dracula-network-bandwidth-interval 30
set -g @dracula-refresh-rate 5
set -g status-position top

# Continuum and Resurrect settings
set -g @continuum-restore 'on'
set -g @continuum-save-interval '5'
set -g @resurrect-capture-pane-contents 'off'
set -g @resurrect-strategy-nvim 'session'
set -g @resurrect-save-shell-history 'off'

# Auto-install TPM if not present
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# Ensure Dracula theme loads (failsafe)
run-shell '~/.tmux/plugins/dracula/dracula.tmux'
