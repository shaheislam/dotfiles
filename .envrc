use flake path:$HOME/dotfiles/nix/global

# Load mise after Nix to ensure mise-managed tools take precedence
# Using direnv integration instead of bash to avoid PROMPT_COMMAND unbound variable error
eval "$(mise activate direnv)"
