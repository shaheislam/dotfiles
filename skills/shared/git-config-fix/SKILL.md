---
name: git-config-fix
description: Fix git configuration issues
argument-hint: issue-description
user-invocable: true
---

Fix git configuration issues described in $ARGUMENTS. Check global and local git configs, SSH settings, credential helpers, and hooks configuration. Verify the fix resolves the issue.
