---
description: Fork the current OpenCode session in this worktree
agent: build
---

Fork the current OpenCode session in this worktree.

This command is normally intercepted by the OpenCode fork command plugin and delegated to OpenCode's native `session_fork` TUI action.

If you see this text as an assistant response, the plugin did not load; use `ctrl+x f` as the fallback native fork keybinding.
