# K9s Configuration
# Main configuration file for k9s

k9s:
  # Refresh rate in seconds
  refreshRate: 2

  # Max number of retries for failed connections
  maxConnRetry: 5

  # Disable cluster info section
  noExitOnCtrlC: false

  # Skip latest version check
  skipLatestRevCheck: false

  # Disable pod counting in headers
  disablePodCounting: false

  # Shell pod configuration for debugging
  shellPod:
    # Image to use for shell pods - using alpine with more tools
    image: alpine:latest

    # Namespace to create shell pods in
    namespace: default

    # Resource limits
    limits:
      cpu: 200m
      memory: 256Mi

  # Image scanning settings
  imageScans:
    # Enable image scanning
    enable: true

    # Exclusions
    exclusions:
      # Namespaces to exclude from scanning
      namespaces: []

      # Labels to exclude
      labels: {}

  # Logger settings
  logger:
    # Number of lines to tail
    tail: 200

    # Buffer size
    buffer: 5000

    # Since seconds (-1 for all)
    sinceSeconds: 300

    # Text wrap
    textWrap: false

    # Show timestamps
    showTime: true

  # Resource thresholds for coloring
  thresholds:
    # CPU thresholds (percentage)
    cpu:
      critical: 90
      warn: 70

    # Memory thresholds (percentage)
    memory:
      critical: 90
      warn: 70

  # Screen dump directory
  screenDumpDir: /tmp/k9s-screens

  # UI settings
  ui:
    # Enable mouse support
    enableMouse: false

    # Disable the k9s logo
    logoless: false

    # Disable breadcrumbs
    crumbsless: false

    # Reactive UI updates
    reactive: true

    # Show icons
    noIcons: false

    # Skin (theme) - Tokyo Night to match terminal
    skin: tokyo-night